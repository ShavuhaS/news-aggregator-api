info:
  name: Remove news location
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{baseUrl}}/news/:id/locations/:locationId"
  params:
    - name: id
      value: ""
      type: path
    - name: locationId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
