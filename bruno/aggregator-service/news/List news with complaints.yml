info:
  name: List news with complaints
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/news/complaints"
  params:
    - name: page
      value: "1"
      type: query
    - name: pageSize
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
