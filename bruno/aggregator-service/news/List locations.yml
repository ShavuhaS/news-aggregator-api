info:
  name: List locations
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/news/locations"
  params:
    - name: page
      value: "1"
      type: query
    - name: pageSize
      value: "20"
      type: query
    - name: search
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
