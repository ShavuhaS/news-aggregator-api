info:
  name: List categories
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/news/categories"
  params:
    - name: page
      value: "1"
      type: query
    - name: pageSize
      value: "20"
      type: query
    - name: search
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
