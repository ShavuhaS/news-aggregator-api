info:
  name: Get news by id
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/news/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
