info:
  name: Get user complaints
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/user/profile/complaints"
  params:
    - name: page
      value: "1"
      type: query
    - name: pageSize
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
