info:
  name: JWKS
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/.well-known/jwks.json"
  params: []
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
