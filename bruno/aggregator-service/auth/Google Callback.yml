info:
  name: Google Callback
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/auth/google/callback"
  params: []
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
