info:
  name: Google Auth
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/auth/google"
  params: []
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
