info:
  name: Get source by id
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/parser/sources/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
