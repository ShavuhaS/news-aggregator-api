info:
  name: Trigger source parse
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/parser/sources/:id/parse"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
